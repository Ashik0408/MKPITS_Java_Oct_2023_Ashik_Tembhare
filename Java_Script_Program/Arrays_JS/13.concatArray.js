let negetiveValue = [-1, -2, -3, -4];
let positiveValue = [1, 2, 3, 4, 5];
let nutralValue = [1, -5, 0];
console.log(nutralValue.concat(positiveValue, negetiveValue)); // concatinate the array and return it
console.log("Concatenated array is : ", nutralValue); // Output
